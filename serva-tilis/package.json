{
  "name": "serva-tilis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler publish --outdir out && npx wrangler tail",
    "logs": "npx wrangler tail",
    "start": "wrangler dev",
    "secrets": "../secrets.sh"
  },
  "dependencies": {
    "langchain": "^0.0.70",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^20.1.0",
    "typescript": "^5.0.3"
  }
}
